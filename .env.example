# Server-only URL from the Google Apps Script Web App deployment.
APPS_SCRIPT_API_URL=

# Must match APP_SECRET in Google Apps Script > Script Properties.
# Generate at least 32 random characters. Never prefix this with NEXT_PUBLIC_.
APPS_SCRIPT_API_SECRET=
